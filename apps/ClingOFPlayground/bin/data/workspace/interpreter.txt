#include "workspace/clingof.hpp"

string typed;
vector<string> completions;
size_t cursor;
cof.interp
cursor=4;

completions.clear();
typed = "ofxNanoVG"

cof.interp->codeComplete("ofxNanoVG",cursor,completions);
ofLog() << "\n\nResults:";
for (string str: completions) { ofLog() << str; }
